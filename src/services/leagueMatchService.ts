import type { ILeagueMatchService } from "./interfaces/ILeagueMatchService";

// todo probably delete this
export class LeagueMatchService implements ILeagueMatchService {
  getMatchesByUserId(id: string, count: number) {
    return [];
  }
}
